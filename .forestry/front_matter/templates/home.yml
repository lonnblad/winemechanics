---
label: Home
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: actions
  type: field_group_list
  fields:
  - name: action
    type: include
    config: {}
    template: action
    label: Action
  config:
    min: 
    max: 
    labelField: 
  label: Actions
- name: background
  type: file
  config:
    maxSize: 250
  label: Background
- name: icon
  type: file
  config:
    maxSize: 250
  label: Icon
pages:
- content/_index.md
