---
label: Home
hide_body: true
fields:
- name: icon
  type: file
  config:
    maxSize: 250
  label: Icon
- name: actions
  type: field_group_list
  fields:
  - name: action
    type: include
    config: {}
    template: action
    label: Action
    hidden: false
  config:
    min: 
    max: 
    labelField: label
  label: Actions
- name: background
  type: file
  config:
    maxSize: 250
  label: Background
- name: text_area
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: true
    schema:
      format: markdown
  label: Text Area
pages:
- content/_index.md
